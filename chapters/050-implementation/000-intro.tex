The following chapter outlines the exact technical implementation of the Ansible operator, including details on resource definitions, code snippets and container build files.
The first section will reason through the selection of the framework used for implementation, as well as the language selection.
The second section details the \glspl{crd} implemented, including their specification, fields and validation rules.
The third section details the reconcile logic on each of the resources, including code listings for important details.
Finally, the chapter will close with details on the executed containers inside the \texttt{CronJob}, how they install dependencies and execute the playbook.
